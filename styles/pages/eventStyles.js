import { StyleSheet } from "react-native";

export const eventStyles = StyleSheet.create({
  container: {
    flex: 1,
  },
  list: {
    margin: 16,
    backgroundColor: "#eee",
    marginBottom: 1,
  },
});
